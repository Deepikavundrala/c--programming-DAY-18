#include<stdio.h>
main()
{
	int n,t,r,rev=0;
	printf("enter a  number");
	scanf("%d",&n);
	t=n;
	while(n!=0)
	{
		r=n%10;
		rev=rev*10+r;
		n=n/10;
	}
	if(rev==t)
	{
		printf("palindrome");
	}
	else
	{
		printf("not a palindrome");
	}
}
	

#include<stdio.h>
main()
{
	int n,a,r,sum=0;
	printf("enter a three digit nnumber");
	scanf("%d",&n);
	a=n;
	while(a!=0)
	{
		r=a%10;
		sum+=r*r*r;
		a=a/10;
	}
	if(sum==n)
	{
		printf("it is an armstrong number");
	}
	else
	{
		printf("it is not an armstrong number");
	}
}
